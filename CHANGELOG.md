# 更新记录

本项目使用语义化版本号：`主版本.功能版本.修订版本`。

## 1.0.4 - 2026-09-10

- 重写自动重连机制：连续 3 次轮询失败后转入后台重连，不再阻塞界面。
- 重连间隔采用指数退避（1s、2s、4s…封顶 30s），持续重试直到恢复，不再限制轮数。
- 重连前检测串口是否仍在线，USB 转串口拔出后插回可自动恢复连接。
- 重连期间可随时手动断开终止；恢复后自动继续轮询。

## 1.0.3 - 2026-09-10

- 增加 `30242` 压缩机 IPM 模块温度参数，支持参数显示、曲线选择、实时曲线和历史导出。

## 1.0.2 - 2026-09-07

- 修正当前功率为 0 时曲线右侧坐标轴显示负值的问题。
- 调整热水温度参数的显示顺序。
- 增加故障列表隐藏/显示按钮。

## 1.0.1 - 2026-09-05

- 将压缩机运行/停止时间及主路、辅路 EXV 开度归入工作状态参数。
- 曲线在拖动或缩放期间继续实时刷新，并保留用户当前视口。
- Excel 导出将参数页、状态页和故障页整合到同一个“曲线数据”工作表。
- 安装器升级时自动关闭旧版程序并重试文件解压，避免文件占用导致安装失败。

## 1.0.0 - 2026-09-05

- 产品名称统一为 W_TB_MS。
- 增加参数页、故障页和状态页实时曲线。
- 曲线数据保留 24 小时，并支持多种显示时间范围。
- 增加每 6 小时自动导出 LOG 和 Excel。
- 增加存档路径记忆与关闭前保存提醒。
- 增加串口自然排序并默认选择最小 COM 序号。
- 增加 GitHub 自动构建、版本打包和本地回档准备。
