# W_TB_MS 使用说明

W_TB_MS 是用于 W 系列热泵设备的 Windows Modbus RTU 监控软件。本说明适用于 `W_TB_MS v1.0.1`。

## 1. 安装与启动

### 使用安装包

运行 `W_TB_MS-v1.0.1-Setup.exe`，选择安装目录后点击“安装”。安装器会自动判断 Windows 架构：

- 64 位 Windows 安装 `win-x64` 程序；
- 32 位 Windows 安装 `win-x86` 程序；
- 安装包自带 .NET 运行环境，不需要另外安装 .NET 8。

安装完成后可以选择立即启动。软件窗口标题显示软件名称和版本号。

### 首次存档路径提示

软件打开后，如果尚未设置存档路径，主界面会显示一次非阻塞提示。点击曲线工具栏中的“存档路径”选择目录即可。该目录用于保存自动存档和手动导出的曲线文件。

## 2. 连接设备

1. 将 USB/RS-485 转换器连接到电脑和热泵设备。
2. 在“串口”下拉框中选择设备串口。串口按 COM 编号从小到大排列，默认选择列表第一项。
3. 选择波特率。软件默认值为 `9600`。
4. 选择从机地址。界面中的 `0xF1`～`0xF4` 对应设备拨码地址。
5. 设置轮询间隔，范围为 `500`～`10000` ms，默认 `2000` ms。
6. 点击“连接”。顶部出现“连接成功”且“更新时间”持续变化后，表示通信正常。

连接成功后软件会自动轮询寄存器、更新参数表、更新状态和故障信息，并记录曲线采样数据。

## 3. 主界面

### 参数表

左侧参数表显示寄存器的文字描述、当前值、单位和操作。寄存器地址和属性列默认隐藏，点击“显示地址/属性”可以展开。

可写参数点击对应行的操作按钮，输入值并确认后写入设备。写入前请确认设备处于允许修改的状态。

### 状态与故障

右上区域显示工作状态、故障汇总和故障报警列表。故障列表使用文字描述，便于现场查看。

## 4. 曲线页面

曲线区域包含三个页面：

- **参数页**：温度、压力、频率、功率等连续数值；
- **状态页**：主机运行状态、设备运行状态、拨码及阀门状态；
- **故障页**：故障页对应的 BIT 状态曲线。

### 选择曲线

右侧树形列表按分组显示曲线。勾选或取消勾选只影响当前页面的显示，不影响数据采集和存档。

BIT 曲线按原始寄存器保存完整数据，显示时再解析对应位，因此不会因为取消勾选而丢失数据。

### 查看数值

鼠标移动到曲线上会显示曲线名称、数值和采样时间。点击并拖动曲线可以查看历史范围；滚轮可以缩放横轴。

拖动或缩放期间，轮询仍会继续采集数据，但暂时不会抢占当前视图。松开鼠标后会刷新一次。普通悬停或点击不会停止曲线自动跟随。

### 时间范围

三个页面共用同一个时间范围选择器，可选择：

`10分钟`、`1小时`、`6小时`、`12小时`、`全部`

选择后会同时作用于参数页、状态页和故障页。

点击“回到当前”可以恢复自动跟随最新数据。

## 5. 数据存档与导出

### 自动存档

软件每 6 小时自动生成一组同名文件：

- `.log`：文本格式通信/曲线记录，可用文本编辑器查看；
- `.xlsx`：Excel 工作簿，可直接用 Microsoft Excel 打开。

自动存档包含全部已采集曲线数据，不受右侧曲线勾选状态影响。BIT 曲线同时保留原始寄存器数据。

软件默认保留约 24 小时的实时曲线内存数据。自动存档文件会持续累积，实际可保存时长取决于磁盘空间。

### 手动导出

在参数页工具栏中：

- 点击“导出LOG”导出当前历史范围；
- 点击“导出Excel”导出当前历史范围；
- 点击“打开历史”读取已有的 LOG/Excel 曲线存档；
- 点击“存档路径”更换自动存档目录。

关闭软件前，如果存在尚未存档的数据，软件会提示是否同时导出 LOG 和 Excel。建议选择导出，避免丢失最后一个自动存档周期内的数据。

## 6. 通信日志

底部“通信日志”显示发送帧、接收帧、系统信息和错误信息。日志采用批量刷新方式，长时间运行时可以减少界面卡顿。点击“隐藏/展开”可以调整日志区域高度。

## 7. 轮询与重连

“轮询(ms)”表示两次自动读取周期之间的间隔。间隔越小，数据更新越快，但串口通信和界面负载也越高。现场使用建议从 `2000` ms 开始调整。

通信连续失败时，软件会自动尝试重连。重连期间不要频繁拔插转换器；如果多轮重连仍失败，请检查串口号、波特率、从机拨码地址和 RS-485 接线。

## 8. 版本回档

软件发布包和版本目录应保留在 `artifacts/` 或 `deployment/versions/` 中。主程序无法启动时，可在项目目录执行：

```powershell
./scripts/Restore-Version.ps1
```

恢复指定版本：

```powershell
./scripts/Restore-Version.ps1 -Version <版本目录名> -Start
```

回档只替换程序文件，不删除存档数据和用户配置。回档前建议关闭正在运行的 W_TB_MS 窗口。

## 9. 常见问题

### 看不到软件窗口

先检查任务栏和 `Alt+Tab`。如果软件进程存在但窗口不可见，结束该进程后重新从安装目录启动。优先使用安装包安装后的程序，不要直接运行被清理或移动过的临时发布目录。

### 更新时间不变化

检查“连接成功”状态、串口是否被其他软件占用，以及 RS-485 接线和设备供电。确认轮询间隔不是被设置为过大值。

### 曲线看起来没有变化

如果设备当前值没有变化，曲线会保持水平，这是正常现象。点击“回到当前”确认没有停留在历史视图；拖动或缩放结束后曲线会自动恢复刷新。

### LOG 能否用 Excel 打开

推荐直接使用同一存档周期生成的 `.xlsx` 文件。`.log` 是文本日志，Excel 可以导入，但需要在 Excel 的“数据/从文本”中指定分隔方式；软件已经同时生成 `.xlsx`，无需额外转换。

## 10. 联系与问题反馈

提交问题时请附上软件版本、Windows 架构（x86/x64）、串口配置、错误时间和相关日志片段。请勿上传包含设备现场敏感信息的完整通信日志。
